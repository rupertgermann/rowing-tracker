// Legacy AI settings component - DEPRECATED
// This component is no longer used and kept only for reference
// All AI settings are now handled through SettingsService.aiSettings
// TODO: Remove this file once confirmed unused

// The original component has been removed to prevent lint errors
// It was using localStorage key 'ai_settings' which is no longer used
// Current implementation uses SettingsService.aiSettings exclusively
