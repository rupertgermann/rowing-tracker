# notes 

- marking plan items as complete works but it does not have a visual effect. i need to reload the page to see the changes

- add a mechanism to automatically collapse long generated chat messages like e.g. the training plan analysis messages or chart explanations. don't collapse responses, only the huge generated chat messages containing excessive content.
